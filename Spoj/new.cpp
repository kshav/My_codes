#include<iostream>
#include<math.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    float k;
    k=log(n)/log(2);
    if (fmod(k, 1) == 0);
    cout<<"HLT";
    system("pause");
    return 0;
}
    
